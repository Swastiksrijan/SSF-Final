services:
  - type: web
    name: ngo-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
    fromDatabase:
      name: ngo-db
      property: connectionString
      key: DB_URL

databases:
  - name: ngo-db
    plan: free
    databaseName: ngo_db
    user: ngo_user

# Note: You will need to link your GitHub repository to Render to use this.
